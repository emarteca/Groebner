# arnborgI (given F and X)
# arnborgIII (given F and X)
# forsman4 (given F and X)
# hell-in (given F and X)
# kiyoshi5 (given F and X)
# p34 (given F and X)


# arnborgI (given F and X) --------------------------------------------------------------

n1 := "arnborgI";

read( "arnborgI");
ord1 := tdeg( X);
B1 := F;


# arnborgIII (given F and X) ------------------------------------------------------------

n2 := "arnborgIII";

read( "arnborgIII");
ord2 := tdeg( X);
B2 := F;


# forsman4 (given F and X) --------------------------------------------------------------

n3 := "forsman4";

read( "forsman4");
ord3 := tdeg( X);
B3 := F;


# hell-in (given F and X) ---------------------------------------------------------------

n4 := "hell-in";

read( "hell-in");
ord4 := tdeg( X);
B4 := F;


# kiyoshi5 (given F and X) --------------------------------------------------------------

n5 := "kiyoshi5";

read( "kiyoshi5");
ord5 := tdeg( X);
B5 := F;


# p34 (given F and X) -------------------------------------------------------------------

n6 := "p34";

read( "p34");
ord6 := tdeg( X);
B6 := F;






totOrds := [ ord1, ord2, ord3, ord4, ord5, ord6];
totBs := [ B1, B2, B3, B4, B5, B6];
exNames := [ n1, n2, n3, n4, n5, n6];